- name: Preferences of variables
  hosts: local
  # vars_files:
  #   - info.yaml
  vars:
    Greet: "Hi from Play"
  # vars_prompt:
  #   - name: Greet
  #     prompt: Enter greeting
  #     private: false
  tasks:
    - name: Task-1
    #   vars:
    #     Greet: "Hi from task"
      ansible.builtin.debug:
        msg: "{{ Greet }}"

#Preference
# 1.command line
# 2.task level variable
# 3.source file
# 4.prompt
# 4. ini file
