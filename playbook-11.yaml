- name: Preferences of variables
  hosts: local
  # vars_files:
  #   - info.yaml
  vars:
    greet: "Hi from Play"
  # vars_prompt:
  #   - name: Greet
  #     prompt: Enter greeting
  #     private: false
  tasks:
    - name: Task-1
      vars:
        greet: "Hi from task"
      ansible.builtin.debug:
        msg: "{{ Greet }}"

#Preference
# 1.command line
# 2.source file
# 3.prompt
# 4. ini file
